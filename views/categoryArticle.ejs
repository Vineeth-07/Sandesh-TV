<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Santesh TV <%= category %> news</title>
    <!-- Include Tailwind CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        margin: 0;
        padding: 0;
      }

      .flex-container {
        display: flex;
        flex-wrap: wrap;
        gap: 4px;
      }

      .article-box {
        width: calc(25% - 8px); /* 25% width for each box with 4px gap */
        border: 1px solid #ddd;
        padding: 8px;
        margin: 4px;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        height: 300px;
      }

      .article-box h2 {
        font-size: 1rem;
        margin-bottom: 8px;
      }

      .article-image {
        flex-grow: 1;
      }

      .article-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 4px;
      }

      .no-image {
        flex-grow: 1;
        background-color: #eee;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 4px;
      }
    </style>
  </head>

  <body class="bg-gray-100">
    <h1 class="text-3xl font-bold p-4 bg-blue-500 text-white">
      <%= title.toUpperCase() %> NEWS
    </h1>

    <div class="flex-container p-4">
      <div class="article-box rounded">
        <a href="<%=category%>/telangana">
          <h2 class="text-xl font-semibold text-gray-700 mb-2">Telangana</h2>
          <div class="article-image">
            <%- include("articleStateItems.ejs",{articles:telanganaArticles})%>
          </div>
        </a>
      </div>
      <div class="article-box rounded">
        <a href="<%=category%>/andhra">
          <h2 class="text-xl font-semibold text-gray-700 mb-2">Andhra Pradesh</h2>
          <div class="article-image">
            <%- include("articleStateItems.ejs",{articles:andhraArticles})%>
          </div>
        </a>        
      </div>
    </div>
  </body>
</html>
