<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image</title>
</head>
<body>
    <div class="image-container">
        <img src="/uploads/<%=filename%>" alt="<%= filename %>">
    </div>
    <div class="navigation-links">
        <% if (prevFilename) { %>
          <a href="/image/<%= prevFilename %>">Previous</a>
        <% } %>
    
        <% if (nextFilename) { %>
          <a href="/image/<%= nextFilename %>">Next</a>
        <% } %>
      </div>
</body>
</html>