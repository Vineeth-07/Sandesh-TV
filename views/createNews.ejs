<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      #content {
        width: 100%;
        padding: 0.5rem;
        border: 1px solid #ccc;
        font-size: 16px;
        line-height: 1.5;
        resize: vertical;
      }
      #content:focus {
        outline: none;
        border: 2px solid #0d9488;
        box-shadow: 20px 20px 60px #00000041, inset -20px -20px 60px #ffffff40;
      }
      label {
        display: block;
        margin-bottom: 0.5rem;
        font-size: 14px;
        color: #333;
      }
      h1 {
        text-align: center;
      }

      .formInput:focus {
        outline: none;
        border: 2px solid #0d9488;
        box-shadow: 10px 10px 60px #00000041, inset -10px -10px 60px #ffffff40;
      }
      .backButton {
        background-color: #0d9488;
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        cursor: pointer;
        transition: background-color 0.3s;
      }

      .backButton:hover {
        background-color: #075e54;
      }
      .createButton {
        background-color: #0d9488;
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 0.375rem;
        cursor: pointer;
        transition: background-color 0.3s, box-shadow 0.3s;
        box-shadow: 5px 5px 20px #00000041, inset -5px -5px 20px #ffffff40;
      }

      .createButton:hover {
        background-color: #075e54;
        box-shadow: 10px 10px 60px #00000041, inset -10px -10px 60px #ffffff40;
      }
    </style>
  </head>
  <body class="bg-gray-50">
    <div class="mb-2"><%- include("header.ejs") %></div>
    <div class="ml-6 mb-2">
      <button class="backButton shadow-md" onclick="goBack()">Go Back</button>
    </div>
    <div
      class="max-w-md mx-auto bg-white p-8 border rounded-lg"
      style="
        box-shadow: 20px 20px 60px #00000041, inset -20px -20px 60px #ffffff40;
      "
    >
      <h1 class="text-xl font-bold mb-4">Add News</h1>
      <form action="/createNews" method="post" enctype="multipart/form-data">
        <div class="mb-4">
          <label for="title" class="block text-sm font-medium text-gray-600"
            >Title</label
          >
          <input
            type="text"
            name="title"
            id="title"
            required
            class="mt-1 p-2 w-full border rounded-lg shadow-md formInput"
          />
        </div>
        <div class="mb-4">
          <label for="state" class="block text-sm font-medium text-gray-600"
            >Select State:</label
          >
          <select
            id="state"
            name="state"
            class="p-2 m-1 border rounded-lg shadow-md formInput"
          >
            <option value="telangana">Telangana</option>
            <option value="andhrapradesh">Andhra Pradesh</option>
            <option value="other">Other</option>
          </select>
        </div>
        <div class="mb-4">
          <label for="content" class="block text-sm font-medium text-gray-600"
            >Content:</label
          >
          <textarea
            id="content"
            name="content"
            rows="10"
            cols="50"
            class="shadow-md rounded-lg"
            required
          ></textarea>
        </div>
        <div class="mb-4">
          <label for="category" class="block text-sm font-medium text-gray-600"
            >Select category:</label
          >
          <select
            class="p-2 m-1 border rounded-lg formInput"
            id="category"
            name="category"
          >
            <option value="cinema">Cinema</option>
            <option value="sports">Sports</option>
            <option value="business">Business</option>
            <option value="spirituality">Spirituality</option>
            <option value="history">History</option>
          </select>
        </div>
        <div class="mb-4">
          <label for="image" class="block text-sm font-medium text-gray-600"
            >Image</label
          >
          <input
            type="file"
            name="image"
            id="image"
            required
            class="mt-1 p-2 w-full border rounded-lg shadow-md formInput"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-500 text-white p-3 rounded-lg createButton"
          style="background-color: #0d9488"
        >
          Create News
        </button>
      </form>
    </div>
    <div class="mt-4"></div>
    <script>
      function goBack() {
        window.history.back();
      }
    </script>
  </body>
</html>
