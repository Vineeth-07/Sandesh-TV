<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .box {
        box-shadow: 20px 20px 60px #00000041, inset -20px -20px 60px #ffffff40;
      }
      @media screen and (max-width: 768px) {
        .responsive-box {
          width: 100%;
          max-width: 100%;
        }
        .articleflex .flex {
          flex-direction: column;
        }
        .gap-10 {
          gap: 1rem;
        }
        .box {
          width: 100%;
          margin-bottom: 1rem;
        }
        .flex-container {
          display: flex;
          flex-wrap: wrap;
          gap: 16px;
          justify-content: center;
        }
      }
      @media screen and (max-width: 480px) {
        .gap-10 {
          gap: 1rem; /* Reduce the gap between boxes on even smaller screens */
        }

        .box {
          padding: 2rem; /* Increase padding for better spacing on smaller screens */
        }
      }
    </style>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
  </head>
  <body>
    <%- include('header.ejs') %> <%- include("navBar.ejs")%>
    <div class="flex p-2 space-x-4 justify-end">
      <a
        href="/allPapers"
        class="flex items-center space-x-2 px-4 py-2 bg-blue-500 text-white rounded"
      >
        <span>All epapers</span>
      </a>
    </div>
    <div><%- include('articleStateItems.ejs') %></div>
  </body>
</html>
