<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <style>
        body {
          margin: 0;
          padding: 0;
        }
    
        .flex-container {
          display: flex;
          flex-wrap: wrap;
          gap: 16px;
          justify-content: center;
        }
    
        .article-box {
          width: 300px; /* Set your preferred width */
          border: 1px solid #ddd;
          padding: 8px;
          margin: 8px;
          box-sizing: border-box;
          display: flex;
          flex-direction: column;
          height: 400px; /* Set your preferred height */
        }
    
        .article-box h2 {
          font-size: 1rem;
          margin-bottom: 8px;
        }
    
        .article-image {
          flex-grow: 1;
        }
    
        .article-image img {
          width: 100%;
          height: 200px;
          object-fit: fill;
          border-radius: 4px;
        }
    
        .no-image {
          flex-grow: 1;
          background-color: #eee;
          display: flex;
          justify-content: center;
          align-items: center;
          border-radius: 4px;
        }
      </style>
</head>
<body>
    <%- include("header.ejs")%>
    <a href="/createArticle">Add</a>
    <div>
        <%- include("articleListItems.ejs") %>
    </div>
    
</body>
</html>